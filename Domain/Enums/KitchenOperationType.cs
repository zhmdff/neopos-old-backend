namespace Domain.Enums;

public enum KitchenOperationType
{
    New = 1,        // Yeni əlavə (+ say)
    Reduced = 2,    // Sayın azaldılması (- say)
    Cancelled = 3   // Məhsulun tam silinməsi (- cəm say)
}